import { Text } from 'react-native';
import { View } from 'react-native';

export default function HomeScreen() {
  return (
    <View style={{flex:1, backgroundColor: '#151718', justifyContent:'center'}}>
      <Text className='text-white'>ola</Text>
    </View>
  );
}
